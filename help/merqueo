mer-user:
 Se conecta al usuario deploy seleccionado. Si solo hay 1 se usa directamente.

mer-infra-up:
 Levanta el código de infraesteuctura para trabajo local.
  1. Hace pull del repositorio de infrastructure. 
  2. Ejecuta el prepare según seleccionado desde una lista desplegable.
  3. Se conecta al usuario.

mer-souce:
 Cambia la rama de despliegue en el proyecto (~/source)
  1. Ejecutar desde el repositorio actual para obtener la rama en uso.
  2. Muestra listado de proyectos del ~/source
  3. Al seleccionado se le define la rama actual o la digitada.

mer-deploy:
 Muestra un listado de los comandos para hacer deploy de los proyectos.
 Se conecta al ambiente, el comando se copia al portapapeles.
 Se pega el contenido y ejecuta la acción.

mer-ssh:
 Realiza conexión ssh al proyecto seleccionado desde una lista.

mer-tunnel:
 Realiza apertura del tunnel seleccionado desde una lista.

mer-deploy-logs:
 Listado los logs generados el /home/deploy-xxx/deployer.
 El seleccionado se abre usando less.

mer-pipeline-php:
 Ejecuta el pipeline de php usando el deployer v2.
 Muestra listado de usuarios disponibles para continuar.

mer-repos:
 Muestra listado de repos y abre el seleccionado con el IDE definido.

